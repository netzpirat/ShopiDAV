#login-header.page-header
  %h1 Install This App
  %h1
    %small This app requires you to login to start using it.

%div
  = form_tag '/login/authenticate', :class => 'form-search' do
    = text_field_tag 'shop', nil, :placeholder => 'Shop URL', :class => 'btn-large'
    = submit_tag 'Install', :class => 'btn btn-primary btn-large'

%p
  = image_tag 'shopify.png'

%br

%p.muted
  Don't have a shop?
  %a{ href: 'https://app.shopify.com/services/partners/api_clients/test_shops' } Create a Test Shop
